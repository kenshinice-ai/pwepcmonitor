<Application x:Class="Pwe.PcMonitor.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:Pwe.PcMonitor.Converters"
             ShutdownMode="OnExplicitShutdown">
    <Application.Resources>
        <!-- The layered windows already support transparency. Keep the PWE
             palette, but let the desktop show through without dimming text. -->
        <SolidColorBrush x:Key="BackgroundBrush" Color="#E60E1729" />
        <SolidColorBrush x:Key="CardBrush" Color="#D9152239" />
        <SolidColorBrush x:Key="StrokeBrush" Color="#B326344B" />
        <SolidColorBrush x:Key="RailBrush" Color="#A62C3A51" />
        <SolidColorBrush x:Key="TextBrush" Color="#F7F5F2" />
        <SolidColorBrush x:Key="MutedBrush" Color="#9098A8" />
        <SolidColorBrush x:Key="AccentBrush" Color="#F5B335" />
        <SolidColorBrush x:Key="HotBrush" Color="#E8654E" />
        <SolidColorBrush x:Key="BadgeHotBrush" Color="#29E8654E" />
        <SolidColorBrush x:Key="BadgeWarmBrush" Color="#29F5B335" />

        <FontFamily x:Key="UiFont">pack://application:,,,/Resources/Fonts/#Inter</FontFamily>
        <FontFamily x:Key="BrandFont">pack://application:,,,/Resources/Fonts/#Playfair Display</FontFamily>

        <!-- Small SVG-style paths kept as WPF geometry so the widget stays
             dependency-free while the compact view can use icon-only labels. -->
        <Geometry x:Key="CpuIconGeometry">M7 7H17V17H7ZM10 10H14V14H10ZM4 9H7M4 12H7M4 15H7M17 9H20M17 12H20M17 15H20M9 4V7M12 4V7M15 4V7M9 17V20M12 17V20M15 17V20</Geometry>
        <Geometry x:Key="MemoryIconGeometry">M5 6H19V18H5ZM8 9H16V11H8ZM8 13H16V15H8M8 4V6M12 4V6M16 4V6M8 18V20M12 18V20M16 18V20</Geometry>
        <Geometry x:Key="GpuIconGeometry">M4 7H20L17 17H7ZM8 11H16M9 14H15</Geometry>
        <Geometry x:Key="PowerIconGeometry">M12 3V11M8 5.2A8 8 0 1 0 16 5.2</Geometry>
        <Geometry x:Key="FanIconGeometry">M12 9.5C9 7 7 8 8.1 10.3C9.4 12.8 11.2 11.9 12 9.5M14.5 12C17 9 16 7 13.7 8.1C11.2 9.4 12.1 11.2 14.5 12M12 14.5C15 17 17 16 15.9 13.7C14.6 11.2 12.8 12.1 12 14.5M12 10A2 2 0 1 0 12 14A2 2 0 1 0 12 10</Geometry>
        <Geometry x:Key="BatteryIconGeometry">M4 7H20V17H4ZM20 10H22V14H20M8 10V14H16V10</Geometry>
        <Geometry x:Key="StorageIconGeometry">M5 4H19A2 2 0 0 1 21 6V18A2 2 0 0 1 19 20H5A2 2 0 0 1 3 18V6A2 2 0 0 1 5 4ZM7 8H17M7 12H17M7 16H12</Geometry>

        <converters:HealthToBrushConverter x:Key="HealthBrush" />
        <converters:HealthToBadgeBrushConverter x:Key="HealthBadgeBrush" />
        <converters:BooleanToVisibilityConverter x:Key="BoolVisibility" />
        <converters:BytesConverter x:Key="BytesConverter" />
        <converters:SensorValueConverter x:Key="SensorValueConverter" />

        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{StaticResource UiFont}" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
        </Style>

        <!-- WPF menus otherwise fall back to Windows light-theme colors even
             when the dashboard is dark, which makes settings unreadable. -->
        <Style TargetType="ContextMenu">
            <Setter Property="Background" Value="{DynamicResource CardBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource StrokeBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontFamily" Value="{StaticResource UiFont}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Padding" Value="4" />
        </Style>

        <Style TargetType="MenuItem">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="Padding" Value="10,7" />
            <Setter Property="FontFamily" Value="{StaticResource UiFont}" />
            <Setter Property="FontSize" Value="12" />
            <Style.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                    <Setter Property="Background" Value="{DynamicResource RailBrush}" />
                    <Setter Property="Foreground" Value="{DynamicResource AccentBrush}" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Separator">
            <Setter Property="Background" Value="{DynamicResource StrokeBrush}" />
            <Setter Property="Margin" Value="6,4" />
        </Style>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource StrokeBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="Padding" Value="14" />
        </Style>

        <Style x:Key="SectionLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{DynamicResource MutedBrush}" />
        </Style>

        <Style x:Key="HeroValueStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="29" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="FontFamily" Value="{StaticResource UiFont}" />
        </Style>

        <Style x:Key="MutedTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource MutedBrush}" />
            <Setter Property="FontSize" Value="11" />
        </Style>

        <Style x:Key="CompactIconStyle" TargetType="Path">
            <Setter Property="Width" Value="13" />
            <Setter Property="Height" Value="13" />
            <Setter Property="Stretch" Value="Uniform" />
            <Setter Property="Stroke" Value="{DynamicResource MutedBrush}" />
            <Setter Property="StrokeThickness" Value="1.5" />
            <Setter Property="StrokeStartLineCap" Value="Round" />
            <Setter Property="StrokeEndLineCap" Value="Round" />
            <Setter Property="StrokeLineJoin" Value="Round" />
            <Setter Property="Fill" Value="Transparent" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style x:Key="DetailIconStyle" TargetType="Path" BasedOn="{StaticResource CompactIconStyle}">
            <Setter Property="Width" Value="15" />
            <Setter Property="Height" Value="15" />
            <Setter Property="Margin" Value="0,0,5,0" />
            <Setter Property="Stroke" Value="{DynamicResource AccentBrush}" />
            <Setter Property="StrokeThickness" Value="1.45" />
        </Style>

        <Style x:Key="SmallButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource MutedBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="9,5" />
            <Setter Property="FontFamily" Value="{StaticResource UiFont}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="{DynamicResource AccentBrush}" />
                    <Setter Property="Background" Value="{DynamicResource RailBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="RefreshRadioStyle" TargetType="RadioButton">
            <Setter Property="Foreground" Value="{DynamicResource MutedBrush}" />
            <Setter Property="FontFamily" Value="{StaticResource UiFont}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Root" Background="Transparent" CornerRadius="7" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{DynamicResource BadgeWarmBrush}" />
                                <Setter Property="Foreground" Value="{DynamicResource AccentBrush}" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource AccentBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ProgressBar">
            <Setter Property="Height" Value="5" />
            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}" />
            <Setter Property="Background" Value="{DynamicResource RailBrush}" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>
    </Application.Resources>
</Application>
